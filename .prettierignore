node_modules
coverage
package-lock.json

# Markdown is hand-wrapped prose. Prettier reflows it, which turns a
# one-line documentation edit into a hundred-line diff and buries the
# actual change.
*.md
